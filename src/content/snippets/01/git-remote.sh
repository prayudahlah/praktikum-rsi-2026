# Hubungkan ke remote pertama kali
git remote add origin <url>

# Kirim commit lokal ke remote
git push -u origin main

# Clone repo yang sudah ada
git clone <url>

# Ambil info dari remote TANPA menggabungkan
git fetch origin

# Ambil + langsung gabungkan
git pull origin main
